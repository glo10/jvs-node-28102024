extends ../layout
block content 
  header
    p
      a(href="/") Retour page d'accueil
    p
      a(href="/users") Retour liste des utilisateurs
  main
    section
      h2=user.name 
      article
        p Email :  #{user.email}
        p Téléphone :  #{user.phone}
        p Adresse : #{user.address.street}  #{user.address.city}
        p CP :  #{user.address.zipcode}
        h3 Localisation
        ul 
          li Latitude : #{user.address.geo.lat}
          li Longitude : #{user.address.geo.lng}
        h3 Entreprise
        p #{user.company.name} - #{user.company.catchPhrase} - #{user.company.bs}