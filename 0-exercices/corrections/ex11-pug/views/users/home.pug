extends ../layout
block content 
  header
    p
      a(href="/") Retour page d'accueil
    h1 Liste des utilisateurs
  main
    ol
      each user in users
        li
          a(href="/users/" + user.id) #{user.name}